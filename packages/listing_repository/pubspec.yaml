name: listing_repository
description: Dart package which manages the authentication domain.
version: 1.0.0

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0
  equatable: ^1.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
